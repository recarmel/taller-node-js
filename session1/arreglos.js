const listaEnteros =[1,2,3,4,5,6];

listaEnteros.forEach((value,indice) => {
    console.log('indice'+ indice, 'Valor:' + value);
})

for (let id = 0; id < listaEnteros.length; id++) {
    const element = listaEnteros[id];
    
}